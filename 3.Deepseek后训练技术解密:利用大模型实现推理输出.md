上一节我们加载模型，然后将文本转换为词源序列输入给模型，模型输出了一个二维数组:
```py
输入对应的词源数量:8
模型输出数据的规格为:torch.Size([8, 151936])
```
这个现实输出是一个二维数组，数组的行数为8，列数为151936.其实后者对应千问基础模型总共具备的词源数量。这个输出表示模型根据输入数据给出了8个词源作为输出。
其中每一行对应词源被作为输出的概率，模型会选择概率最大的那个词源当做本次回复中对应的词源。我们使用代码看看模型输出的数据内容:
```py
last_token=output_tensor_fmt[-1]
print(last_token)
```
运行上面代码后给出如下输出:
```py
tensor([ 8.6250, 10.3125, 11.1875,  ..., -1.6797, -1.6797, -1.6797],
       dtype=torch.bfloat16)
```
上面显示数据就是模型给出的输出中，最后一个元素对应每个词源应该作为输出结果的概率。问题在于如果是概率的话，为何上面的数值还有负数呢，这是因为上面的输出还没有经过softmax进行归一化处理。
但这并不妨碍我们取数组中数值最大那个元素的下标对应的词源，我们执行如下代码:
```py
print(torch.argmax(last_token, dim=-1, keepdim=True))
print(tokenizer.decode([33108]))
```
运行上面代码后给出结果为:
```py
tensor([33108])
和
```
上面输出结果的意思是，下标为33108的元素对应的数值最大，那意味着词源编号为33108的词源为模型输出字符串中第八个元素的内容,这个词源对应的汉字就是"和".现在我们已经能够把模型输出的一个向量转换成了对应的文字，下面我们把模型所有输出的向量转换为文字看看效果:
```py
#设置为推理模式能加快模型输出速度，因为他不需要对模型参数梯度变化等信息进行记录
@torch.inference_mode()
def generate_text_basic_stream(model, token_ids, max_new_tokens, eos_token_id=None):
  model.eval() #设置模型为推理模式
  for _ in range(max_new_tokens):
    #模型一次生成一个新的词源
    out = model(token_ids)[:,-1]
    #选取概率值最大的词源作为本次输出
    next_token = torch.argmax(out, dim=-1, keepdim=True)
    #eos_token_id表示模式输出结束了
    if (eos_token_id is not None and torch.all(next_token==eos_token_id)):
      break

    yield next_token
    #将当前输出词源添加到末尾然后将整个词源集合作为下一次的输入
    token_ids = torch.cat([token_ids, next_token], dim=-1)
```
上面代码的逻辑是，将初始提示词转换的词源序列输入给模型，模型会创建下一个词源，我们调用一次上面的代码就比较清楚其逻辑了:
```py
#给定一个问题，然后让模型输出答复，设置答复最大长度为100个词源
prompt="一句话解释什么是大模型"
input_token_ids_tensor = torch.tensor(
    tokenizer.encode(prompt),
    device="cpu"
).unsqueeze(0)
max_new_tokens = 100

for token in generate_text_basic_stream(model=model, token_ids=input_token_ids_tensor, max_new_tokens=max_new_tokens):
  token_id = token.squeeze(0).tolist()
  print(tokenizer.decode(token_id), end="", flush=True)
```
上面代码我们先给出提示词"一句话解释什么是大模型"，然后代码先将其转换为对应的词源，然后输入给模型，模型会输出接着这句话后的一系列词源，我们设置了大模型最多输出100个词源就停止。上面代码运行后给出结果如下:
```py

大模型，也称为大型语言模型（Large Language Model, LLM），是指在训练过程中使用了大量数据（如文本、图像、音频等）来学习语言规律的机器学习模型。这些模型通过深度学习技术，能够理解和生成人类语言，具备了与人类相似的自然语言处理能力。大模型通常具有较高的准确率和泛化能力，能够处理更复杂、更广泛的话题，如情感分析、文本生成、对话系统等。随着
```
我们看上面输出比较有意思的地方在于，第一个词源是一个换行符。也就是模型直到提示词是一个问句，它给出答案前先输出一个换行符，这样答案就不会跟前面的问题串并成一行。在前面的代码中，我们每次只取最后一个提示词，这是因为模型的输出会包含它的输入，基本逻辑如下:
```py
输入:一句话解释什么是大模型
输出:一句话解释什么是大模型\n

输入:一句话解释什么是大模型\n
输出:一句话解释什么是大模型\n大

输入:一句话解释什么是大模型\n大
输出:一句话解释什么是大模型\n大模

...
```
可以看到每次输出都包含了上一次输入的所有内容，同时模型多输出了一个新的词源补充在输入的后面，这也是为何上面代码中我们每次都使用模型输出内容的郋一个词源作为输出。

另外还有一些值得一说的是，前面代码使用到了squeeze和unsqueeze操作，这些操作用来给高维向量或者张量执行维度的扩展和压缩，具体我们看一些例子才容易明白:
```py
test=torch.tensor([1,2,3])
print(test.shape)
print(test)#这里是一维向量
unsqueeze_test=test.unsqueeze(0)
print(unsqueeze_test.unsqueeze(0))#这里转换为维度为(1,3)的向量，也就是它是二维矩阵，该矩阵是一行三列
print(unsqueeze_test.shape)
```
上面代码输出如下:
```py
torch.Size([3])
tensor([1, 2, 3])
tensor([[[1, 2, 3]]])
torch.Size([1, 3])
```
同理看看squeeze的用法：
```py
#squeeze操作结果与unsqueeze相反
test=torch.tensor([[1,2,3]])#初始化一个一行三列的二维矩阵
print(test.shape)
squeeze_test=test.squeeze(0)#压缩第0个维度，也就是最高那个维度
print(squeeze_test.shape)
print(squeeze_test) #变成一个一维向量
```
上面输出如下:
```py
torch.Size([1, 3])
torch.Size([3])
tensor([1, 2, 3])
```
本节我们使用提示词字符串转换为词源字符串，然后输入大模型得到一系列张量，这些张量的元素个数就是所有词源的总量，每个元素值对应本次输出对应词源的概率，因此我们选择数值最大的元素对应的下标就是本次输出的词源，然后将其转换为汉字就得到最终输出。从代码运行可以看到，输出结果的过程是一个漫长的流程，如果统计最终完成时间，我使用colab的cpu来跑千问基础模型得到最终输出的时间大纲是10分钟以上，这个耗时比较长，原因之一是我走的是cpu，如果使用GPU那么时间应该能缩短到两三分钟以内，下一节我们看看如何使用缓存来有效的提升模型输出效率
