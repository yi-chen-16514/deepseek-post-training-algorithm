在了解完词源器的使用后，我们看看如何加载和运行基础模型，这个基础模型虽然说已经训练好，这相当于它自身储备了大量的知识，但是它不具备思考和推理能力，如何将它已经拥有的知识储备尽可能的进行应用并解决实际问题，
那是我们后训练需要解决的问题。这就好像一个人习得九阳神功具备了强大的内力，但内力如何使用并形成打击力，那还是需要相应的外在招式。

我们还是以千问3b作为基础模型，该模型具备0.6b的参数，这个模型大小比较适合于运行在个人笔记本电脑上，就算我们没有专业的GPU，由于模型大小适中，在普通消费级电脑上运行也不会有太大问题。首先我们用下面代码将基础模型下载到本地：
```py

def download_qwen3_base(out_dir="."):
    files = {
        "base": {"model": "qwen3-0.6B-base.pth", "tokenizer": "tokenizer-base.json"},
        "reasoning": {"model": "qwen3-0.6B-reasoning.pth", "tokenizer": "tokenizer-reasoning.json"},
    }
    #下载基础模型
    kind="base"

    repo = "rasbt/qwen3-from-scratch"
    hf_fmt = "https://huggingface.co/{repo}/resolve/main/{file}"
    backup_root = "https://f001.backblazeb2.com/file/reasoning-from-scratch/qwen3-0.6B"
    targets = ["model", "tokenizer"]

    for key in targets:
        fname = files[kind][key]
        primary = hf_fmt.format(repo=repo, file=fname)
        backup = f"{backup_root}/{fname}"
        download_file(primary, out_dir=out_dir, backup_url=backup)
```
上面代码主要是从抱脸网站对应的目录下将模型下载到本地，将它当做一个API看就可以，不用具体了解其逻辑。执行上面函数就会把千问3基础模型对应的参数下载到本地:
```py
download_qwen3_base()
```
执行上面代码后，能够看到本地下载了两个文件，分别是qwen3-0.6B-base.pth，这个文件包含了模型训练好的参数，另一个就是前面我们用过的词源器基础文件。
